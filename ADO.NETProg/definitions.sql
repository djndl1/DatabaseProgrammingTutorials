-- SQLALCHEMY.TKEYUSER definition

CREATE TABLE "TKEYUSER" (
    "ID_USER" NUMBER(*, 0) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    "DT_NAME" VARCHAR2(40),
    "DT_MAX_SIZE" NUMBER(*, 0),
    PRIMARY KEY ("ID_USER")
);

CREATE TABLE "tkeyuser" (
    "iduser" NUMBER(*, 0) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    "dtname" VARCHAR2(40),
    "dtmaxSize" NUMBER(*, 0),
    PRIMARY KEY ("iduser")
);


CREATE TABLE "SQLALCHEMY"."TS_TEST" 
   (	"TS_TZ" TIMESTAMP (6) WITH TIME ZONE, 
	"TS_NORMAL" TIMESTAMP (6), 
	"TS_LTZ" TIMESTAMP (6) WITH LOCAL TIME ZONE, 
	"INTERVAL_SPAN" INTERVAL DAY (1) TO SECOND (6)
   ) ;